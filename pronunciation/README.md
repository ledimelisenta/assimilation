# Pronunciation audio

Place generated Korean pronunciation files here. Filenames are defined in `src/data/pronunciation_audio_manifest.json`.

Until the files are added, development may use browser `SpeechSynthesis` with `lang = "ko-KR"` as a temporary fallback. Production should prefer the supplied audio files.
